{
  "name": "sotdl-spell-cards",
  "private": true,
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web install && npm --prefix web run build",
    "preview": "npm --prefix web run preview"
  }
}
